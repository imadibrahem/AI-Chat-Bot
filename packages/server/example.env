OPENAI_API_KEY =  // OPENAI API KEY (fill it with a random string if you are using oss as LM Studio ignores it)
LM_BASE_URL = // LM Studio local endpoint if you are using oss and LM (comment in /server/index.ts if you are using online model)